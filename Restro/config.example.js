// Configuration — copy to config.js and fill in real values.
// config.js is gitignored and will NOT be committed.
const CONFIG = {
    // Google OAuth Client ID (from Google Cloud Console > APIs & Services > Credentials)
    // This is a PUBLIC value by design for Google Identity Services.
    GOOGLE_CLIENT_ID: 'YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com',

    // Admin credentials (used for admin.html panel login)
    // IMPORTANT: For production, replace this with a real backend auth system.
    ADMIN_EMAIL: 'admin@restaurant.com',
    ADMIN_PASSWORD: 'CHANGE_ME_TO_A_STRONG_PASSWORD',
};
